{
  "name": "execution-boundary-interpretation",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "ncc build dist/index.js -o dist",
    "package": "npm run build && npm run bundle"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0",
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.3.0"
  }
}