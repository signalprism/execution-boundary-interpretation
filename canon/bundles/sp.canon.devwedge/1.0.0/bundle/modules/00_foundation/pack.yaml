schema: sp.canon.pack.v0_1

pack_id: "sp-dev-foundation"
pack_version: "0.1.0"
type: "foundation"

scope:
  applies_to:
    surfaces:
      - "github.pull_request"
      - "agent.tool_call"

domain:
  domain_pack_id: "DP-DEV-GOVERNANCE"
  domain_pack_version: "0.1.0"
  refs:
    entities: "domain/entities.yaml"
    relationships: "domain/relationships.yaml"
    expectations: "domain/expectations.yaml"
    interpretive_rules: "domain/interpretive_rules.yaml"
    narrative_fragments: "domain/narrative_fragments.yaml"
    domain_pack: "domain/domain_pack.yaml"

prisms:
  - prism_id: "PRISM-DEV-PRIMARY"
    prism_version: "0.1.0"
    ref: "prisms/dev_primary.yaml"

schemas:
  - "schemas/interpretation-artifact-v2.json"

provenance:
  authored_by: "Signal & Prism"
  ai_assisted: true
  curated: true
